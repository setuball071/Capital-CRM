node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
attached_assets/*.csv

# Variáveis de ambiente — NUNCA commitar
.env
.env.local
.env.*.local
.env.production
.env.staging
*.env

.vercel

# Memórias do projeto (uso local/Claude — não enviar pro Replit)
memoria/
